language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-mobile' ]; then cd .. && eval "mv $currentfolder generator-angular-mobile" && cd generator-angular-mobile; fi

